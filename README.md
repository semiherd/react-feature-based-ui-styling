This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Feature Based UI Component Styling

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

## Learn More
Adding new features as custom hook or css class is easier to reuse as a new feature on different ui components.

## Deploy on Vercel
https://react-feature-based-ui-styling.vercel.app/
